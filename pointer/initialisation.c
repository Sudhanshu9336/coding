#include<stdio.h>
int main(){
    int a=10;
    
    printf("the address of a is=%d",&a);
    printf("the address of a is=%d",*&a);
    int *j;
    j=&a;
    printf("\n the address of a is=%d",j);
    printf("\n the address of a is=%d",&j);
    int *k;
printf("\n the address of a is=%d",k);
int b=5;
cout<<

}